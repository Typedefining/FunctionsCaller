# FunctionsCaller

FunctionsCaller 是一个基于 C++17 的简单函数式表达式语言实验项目，当前包含：

- 词法分析和递归下降语法分析；
- AST 构建和语义绑定；
- 基于 AST 的解释执行；
- 基于 LLVM 的 LLVM IR 生成；
- 函数调用、变量、条件表达式、循环表达式和字符串拼接。

当前语言仍处于实验阶段，README 中的语法以 `Calculater/scanner.cc` 的实际解析逻辑为准。

## 构建环境

- Windows；
- Visual Studio C++；
- CMake 3.20 或更高版本；
- C++17；
- LLVM 18，推荐通过 vcpkg 安装。

## 构建和测试

```powershell
cmake -S . -B build-debug `
  -DLLVM_DIR="${llvm_dir}"

cmake --build build-debug --config Debug --parallel 16

ctest --test-dir build-debug -C Debug --output-on-failure
```

安装依赖：

```powershell
vcpkg install --triplet x64-windows
```

构建完成后，主要程序位于：

```text
build-debug/Calculater/Debug/FunctionsCalculater.exe
```

测试程序位于：

```text
build-debug/Calculater/Debug/FunctionCallASTTest.exe
```

## 语言词法

### 标识符

标识符必须以字母开头，后续只能包含字母或数字：

```text
add
value1
abc123
```

当前不支持下划线：

```text
my_function    # 不支持
```

### 关键字

当前关键字为：

```text
def if then else for in var
```

### 整数和浮点数

```text
123
0
3.14
0.5
```

当前没有完整的小数格式校验，不建议使用多个小数点。

### 字符串

字符串使用双引号：

```text
"hello"
"hello world"
```

当前不支持字符串转义，例如 `\n` 不会被转换为换行符。

### 注释

使用 `#` 添加单行注释：

```text
# this is a comment
def add(a:int, b:int) a + b # function definition
```

注释持续到换行或文件结束。

## 表达式

基本表达式包括：

```text
number
string
identifier
function-call
(expression)
if-expression
for-expression
variable-declaration
```

### 二元运算符

| 运算符 | 优先级 | 说明 |
| --- | ---: | --- |
| `=` | 5 | 赋值 |
| `<` | 9 | 小于比较 |
| `+` | 10 | 数值加法或字符串拼接 |
| `-` | 10 | 减法 |
| `*` | 20 | 乘法 |
| `/` | 20 | 除法 |

例如：

```text
a + b * 2
(a + b) * 2
a = 10
```

`*` 和 `/` 的优先级高于 `+` 和 `-`。赋值表达式的左侧必须是变量。

当前不支持以下运算符：

```text
>
<=
>=
==
!=
&&
||
!
```

当前也不支持一元负号：

```text
-1       # 不支持
-(a + b) # 不支持
```

可以使用二元表达式替代：

```text
0 - 1
```

## 函数定义

函数定义格式：

```text
def functionName(parameterList) expression
```

参数格式为：

```text
name:type
```

参数之间必须使用逗号分隔：

```text
def add(a:int, b:int) a + b
```

支持的参数类型：

```text
int
double
string
```

零参数函数：

```text
def getValue() 42
```

多参数函数：

```text
def mix(a:int, b:double, c:string) a
```

## 函数调用

函数调用的参数之间使用逗号：

```text
add(1, 2)
getValue()
```

参数可以是表达式或其他函数调用：

```text
add(1 + 2, 3 * 4)
add(square(2), 3)
```

## 变量声明

变量声明格式：

```text
var name:type = expression
```

示例：

```text
def calculate(x:int)
    var y:int = x + 10;
    y
```

函数内变量必须先声明后使用：

```text
def test()
    var x:int = 10;
    x + 1
```

顶层变量使用 `var name:type = expression` 声明，生命周期覆盖整个程序执行过程：

```text
var counter:int = 10;
counter = counter + 1;
counter
```

函数内的局部变量优先于同名顶层变量；如果当前函数没有同名局部变量，变量引用会解析到顶层变量。顶层变量的初始化按照源代码顺序在顶层运行时执行，因此当前规则是先声明后使用。

## 条件表达式

条件表达式格式：

```text
if condition then expression else expression
```

示例：

```text
def max(a:int, b:int)
    if a < b then b else a
```

`else` 是必需的：

```text
if a < b then b   # 不完整
```

当前没有独立的代码块分隔符。

## 循环表达式

循环表达式格式：

```text
for variable = start, end [, step] in expression
```

示例：

```text
def loop(n:int)
    for i = 0, i < n, 1 in i
```

步长可以省略，解释器和 LLVM codegen 默认步长为 `1`：

```text
for i = 0, i < 10 in i
```

当前循环没有可靠的显式 `end` 终止语法。虽然 token 枚举中存在 `tok_end`，但 lexer 当前没有将 `end` 识别为关键字。

## 表达式序列和顶层输入

多个表达式可以使用分号分隔：

```text
def update()
    var x:int = 1;
    x = x + 1;
    x
```

顶层可以包含多个函数定义和函数调用：

```text
def add(a:int, b:int) a + b;
def sub(a:int, b:int) a - b;
add(10, 3)
```

换行本身只是空白，不等价于分号。

当输入只有一个 AST 节点时，`FCScanner::analysis()` 返回该节点；当输入包含多个节点时，会返回 `FCProgramAST`。

## 当前语言限制

当前版本暂不支持：

- `return` 语句；
- `extern` 声明；
- 布尔字面量；
- 完整的比较和逻辑运算；
- 一元运算；
- 数组、结构体和对象；
- 显式代码块；
- 完整的字符串转义；
- 带下划线的标识符；
- 可靠的 `end` 块结束符。

解释执行和 LLVM IR 生成目前都以函数的最后一个表达式作为返回值。LLVM 生成的顶层 `__fc_main` 当前要求最终表达式可以转换为整数。

## C++ API

解析：

```cpp
FCScanner scanner;
auto ast = scanner.analysis("def add(a:int, b:int) a + b; add(2, 3)");
```

解释执行：

```cpp
FCExprClass::FCEvaluationContext context;
auto result = FCExprClass::evaluate(ast.get(), context);
```

生成 LLVM IR：

```cpp
FCExprClass::FCCodegenContext codegenContext("ExampleModule");
auto value = FCExprClass::codegen(ast.get(), codegenContext);
```

生成的模块可以通过 LLVM 验证：

```cpp
llvm::verifyModule(*codegenContext.module, &llvm::errs());
```

## 项目结构

```text
Calculater/
├── token.h/.cc       AST 和核心语义数据
├── semantic.h/.cc    FCSemanticContext，解析期作用域和声明信息
├── scanner.h/.cc     词法分析和递归下降解析
├── runtime.h         解释执行所需的运行时值和调用帧
├── evaluator.h/.cc   AST 解释执行
├── codegen.h/.cc     LLVM IR 生成
└── call_ast_test.cc  解析、解释执行和 LLVM codegen 测试
```

CMake 目标按职责划分为：

```text
FunctionsCore
FunctionsSemantic
FunctionsFrontend
FunctionsEvaluator
FunctionsCodegen
```

其中 `FunctionsCore` 不依赖 LLVM；LLVM 依赖只由 `FunctionsCodegen` 引入。
