# FunctionsCaller

FunctionsCaller 是一个基于 C++17 的函数式表达式语言实验项目，实现了从词法分析、语法分析、语义绑定到解释执行和 LLVM IR 生成的完整前端链路。

当前包含：

- 词法分析和递归下降语法分析（`FCScanner`）；
- AST 构建，并在解析期完成变量到声明的静态绑定（作用域系统）；
- 基于 AST 的树遍历解释器（全局帧 + 函数调用帧）；
- 基于 LLVM 的 IR 生成与模块验证；
- 函数定义/调用、递归、全局与局部变量、赋值、条件表达式、循环表达式、`{}` 块表达式、字符串拼接；
- 覆盖各模块的单元测试套件与行覆盖率脚本。

当前语言仍处于实验阶段，README 中的语法描述以 `Calculater/scanner.cc` 的实际解析逻辑为准。

## 构建环境

- Windows；
- Visual Studio C++（含 C++ 工具链；本仓库使用 Visual Studio 2026 生成器配置过，2022 亦可）；
- CMake 3.20 或更高版本；
- C++17；
- LLVM 18，推荐通过 vcpkg 安装。

## 构建和测试

依赖由 `vcpkg.json` 声明（`llvm`），默认 triplet 安装：

```powershell
vcpkg install --triplet x64-windows
```

仓库自带 `triplets/x64-windows-debug.cmake`（debug 构建类型 triplet），也可使用：

```powershell
vcpkg install --triplet x64-windows-debug
```

配置、构建、测试：

```powershell
cmake -S . -B build-debug `
  -DLLVM_DIR="${llvm_dir}"

cmake --build build-debug --config Debug --parallel 16

ctest --test-dir build-debug -C Debug --output-on-failure
```

`LLVM_DIR` 指向 vcpkg 安装的 LLVM 配置目录。使用本仓库内的 `vcpkg_installed` 时示例：

```powershell
cmake -S . -B build-debug `
  -DLLVM_DIR="D:\Code\github\FunctionsCaller\vcpkg_installed\x64-windows\share\llvm"
```

构建完成后，主要程序位于：

```text
build-debug/Calculater/Debug/FunctionsCalculater.exe
```

该程序（`main.cc`）目前只是解析一段硬编码示例的最小入口。测试程序位于：

```text
build-debug/Calculater/Debug/FunctionsUnitTest.exe
```

## 语言词法

### 标识符

标识符必须以字母开头，后续只能包含字母或数字：

```text
add
value1
abc123
```

当前不支持下划线：

```text
my_function    # 不支持
```

### 关键字

当前关键字为：

```text
def if then else for in var
```

### 整数和浮点数

```text
123
0
3.14
0.5
```

当前没有完整的小数格式校验，不建议使用多个小数点。

### 字符串

字符串使用双引号：

```text
"hello"
"hello world"
```

当前不支持字符串转义，例如 `\n` 不会被转换为换行符。

### 注释

使用 `#` 添加单行注释：

```text
# this is a comment
def add(a:int, b:int) { a + b } # function definition
```

注释持续到换行或文件结束。

## 表达式

基本表达式包括：

```text
number
string
identifier
function-call
(expression)
{ block }
if-expression
for-expression
variable-declaration
```

### 二元运算符

| 运算符 | 优先级 | 说明 |
| --- | ---: | --- |
| `=` | 5 | 赋值 |
| `<` | 9 | 小于比较 |
| `+` | 10 | 数值加法或字符串拼接 |
| `-` | 10 | 减法 |
| `*` | 20 | 乘法 |
| `/` | 20 | 除法 |

例如：

```text
a + b * 2
(a + b) * 2
a = 10
```

`*` 和 `/` 的优先级高于 `+` 和 `-`。赋值表达式的左侧必须是变量（`1 = 2` 会在解释/编译阶段报错）。

当前不支持以下运算符：

```text
> <= >= == != && || !
```

当前也不支持一元负号：

```text
-1       # 不支持
-(a + b) # 不支持
```

可以使用二元表达式替代：

```text
0 - 1
```

### 块表达式

使用 `{}` 作为块级语法，块内表达式必须用 `;` 分隔（允许末尾 `;`，也允许空块）：

```text
{ var x:int = 1; x + 1 }
```

块的值是最后一个表达式的求值结果。每个块引入一个新的作用域，块可以嵌套：

```text
def nested() { { 1 }; { 2 }; 3 }
```

## 函数定义

函数定义格式（函数体必须是一个 `{}` 块）：

```text
def functionName(parameterList) { body }
```

参数格式为：

```text
name:type
```

参数之间必须使用逗号分隔：

```text
def add(a:int, b:int) { a + b }
```

支持的参数类型：

```text
int
double
string
```

零参数函数：

```text
def getValue() { 42 }
```

多参数函数：

```text
def mix(a:int, b:double, c:string) { a }
```

函数的返回值是函数体块中最后一个表达式的值。空函数体 `{}` 在语法上可解析，但 LLVM codegen 会失败。

## 函数调用

函数调用的参数之间使用逗号：

```text
add(1, 2)
getValue()
```

参数可以是表达式或其他函数调用：

```text
add(1 + 2, 3 * 4)
add(square(2), 3)
```

参数个数与类型在解释执行 / codegen 阶段校验（例如 `f(1, 2)` 对单参数函数 `f` 会报错）。

## 变量声明

变量声明格式：

```text
var name:type = expression
```

示例：

```text
def calculate(x:int) { var y:int = x + 10; y }
```

函数内变量必须先声明后使用：

```text
def test() { var x:int = 10; x + 1 }
```

- 顶层（非函数作用域内）的 `var` 声明为全局变量，生命周期覆盖整个程序执行过程：

```text
var counter:int = 10;
counter = counter + 1;
counter
```

  全局变量的初始化按照源代码顺序在顶层运行时执行，因此当前规则是先声明后使用。

- 函数内的 `var` 声明为局部变量，局部变量优先于同名全局变量（遮蔽）；如果当前函数没有同名局部变量，变量引用会解析到全局变量。

- 同一作用域内重复声明同名变量会报错（全局或局部均如此）。

- 需要注意：顶层作用域内的任意位置（包括顶层块 `{ var g:int = 1; g }` 或顶层 `if` 分支）出现的 `var` 声明，由于不在函数作用域内，都会被注册为全局变量。

## 条件表达式

条件表达式格式：

```text
if condition then expression else expression
```

示例：

```text
def max(a:int, b:int) { if a < b then b else a }
```

`else` 是必需的：

```text
if a < b then b   # 不完整
```

条件必须是数值（`int` 或 `double`），非 0 即为真：

```text
if 1.5 then 1 else 2   # 求值为 1
if 0.0 then 1 else 2   # 求值为 2
```

## 循环表达式

循环表达式格式：

```text
for variable = start, condition [, step] in expression
```

示例：

```text
def loop(n:int) { for i = 0, i < n, 1 in i }
```

- 循环变量为 `int` 类型，循环前先求值 `condition`，非 0 则继续迭代；
- 步长可以省略，解释器和 LLVM codegen 默认步长为 `1`：

```text
for i = 0, i < 10 in i
```

- 循环表达式的值是最后一次循环体的求值结果：

```text
def loopReturn(n:int) { for i = 0, i < n, 1 in i }; loopReturn(3)   # 求值为 2
```

循环支持嵌套：

```text
def nf(n:int) { var s:int = 0; for i = 0, i < n, 1 in for j = 0, j < n, 1 in s = s + i; s }
```

## 表达式序列和顶层输入

多个表达式可以使用分号分隔：

```text
def update() { var x:int = 1; x = x + 1; x }
```

括号内也可以是表达式序列：

```text
(1; 2; 3)
(x = x + 2; x * 3)
```

顶层可以包含多个函数定义和表达式：

```text
def add(a:int, b:int) { a + b };
def sub(a:int, b:int) { a - b };
add(10, 3)
```

换行本身只是空白，不等价于分号。

当输入只有一个 AST 节点时，`FCScanner::analysis()` 返回该节点；当输入包含多个节点时，会返回 `FCProgramAST`。

## 当前语言限制

当前版本暂不支持：

- `return` 语句；
- `extern` 声明；
- 布尔字面量；
- 完整的比较和逻辑运算（仅 `<`）；
- 一元运算（含一元负号）；
- 数组、结构体和对象；
- 字符串转义；
- 带下划线的标识符；
- `end` 关键字（token 已定义但未生效）。

其他已知行为：

- 循环变量仅支持 `int`，浮点循环变量会报错；
- LLVM 生成的顶层 `__fc_main` 会把最终表达式 cast 成整数，cast 失败时回退为常量 `0`；
- 字符串拼接的 LLVM codegen 调用 `strlen` / `malloc` / `strcpy` / `strcat`（作为外部函数声明），拼接结果不释放内存，且链接时需要这些 libc 符号；
- `vcpkg.json` 中的 `name` 字段仍是旧名称（`sampler-array-shader-compiler`），不影响构建。

解释执行和 LLVM IR 生成都以函数的最后一个表达式（块的最后一条语句）作为返回值。

## 单元测试和覆盖率

构建并运行全部测试：

```powershell
cmake --build build-debug --config Debug --parallel 16
ctest --test-dir build-debug -C Debug --output-on-failure
```

测试程序 `FunctionsUnitTest`（`unit_test.cc`）是一个自包含的综合测试套件，覆盖：

- 词法 / 解析（含错误路径）；
- 语义上下文（作用域、槽位分配、函数注册）；
- 解释执行（数值、字符串、函数、递归、全局状态、循环、压力测试）；
- 函数注册表 API；
- LLVM codegen 及 codegen 错误路径；
- AST 节点 `info()` 输出与公开 API 边界。

在 Windows + Visual Studio 环境中，可以使用仓库中的脚本收集生产实现文件（`*.cc`）的行覆盖率。脚本会排除测试驱动（`unit_test.cc` / `call_ast_test.cc`）和 `main.cc`，并在低于 95% 时返回失败：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\coverage.ps1 `
    -BuildDir .\build-debug -Configuration Debug
```

## C++ API

解析：

```cpp
FCScanner scanner;
auto ast = scanner.analysis("def add(a:int, b:int) { a + b }; add(2, 3)");
```

解释执行：

```cpp
FCExprClass::FCEvaluationContext context(scanner.semanticContext().getCompiledProgram());
auto result = FCExprClass::evaluate(ast.get(), context);
// result.type 为 FCValueCategory::Integer / Floating / String / Dangle
```

生成 LLVM IR：

```cpp
FCExprClass::FCCodegenContext codegenContext("ExampleModule", scanner.semanticContext().getCompiledProgram());
auto value = FCExprClass::codegen(ast.get(), codegenContext);
```

生成的模块可以通过 LLVM 验证：

```cpp
llvm::verifyModule(*codegenContext.module, &llvm::errs());
```

## 项目结构

```text
Calculater/
├── token.h/.cc       AST 节点（FCExprAST 家族）与核心数据结构
├── semantic.h/.cc    FCSemanticContext：作用域栈、变量槽位、函数注册
├── scanner.h/.cc     FCScanner：词法分析和递归下降解析
├── runtime.h         解释器运行时值表示（FCValue / Frame）
├── evaluator.h/.cc   解释执行（evaluate）与 FCFunctionRegistry
├── codegen.h/.cc     LLVM IR 生成（codegen）
├── unit_test.cc      综合测试套件（FunctionsUnitTest）
└── main.cc           最小可执行入口（FunctionsCalculater，硬编码示例）

tools/
└── coverage.ps1      行覆盖率收集脚本（默认阈值 95%）

triplets/
└── x64-windows-debug.cmake   vcpkg debug 构建 triplet

CMakeLists.txt        根 CMake 配置
vcpkg.json            依赖清单（llvm）
```

CMake 目标按职责划分为：

```text
FunctionsCore        AST 与核心数据结构（不依赖 LLVM）
FunctionsSemantic    语义上下文 / 作用域
FunctionsFrontend    词法与解析（scanner）
FunctionsEvaluator   解释执行
FunctionsCodegen     LLVM IR 生成（唯一引入 LLVM 依赖的目标）
```

其中 `FunctionsCore` 不依赖 LLVM；LLVM 依赖只由 `FunctionsCodegen` 引入。